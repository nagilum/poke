namespace Poke.Core;

public enum ConsoleColorEx
{
    ResetColor,
    NextColorIsBackground
}

public enum RenderingEngine
{
    Chromium,
    Firefox,
    Webkit
}

public enum QueueItemType
{
    Asset,
    External,
    Resource
}